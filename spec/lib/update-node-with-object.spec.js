'use strict'

var updateNode = require('../../lib/update-node')

describe('update-node-with-object', function () {
  describe('When given a className', function () {
    it(' should add the new class to the existing classes')
  })
  describe('When data is a string', function () {
    it('should update the value')
  })
  describe('When data is a number', function () {
    it('should update the value')
  })

  describe('When given multiple selectors (className and innerHTML)', function () {
    it('should update both the values')
  })
})
